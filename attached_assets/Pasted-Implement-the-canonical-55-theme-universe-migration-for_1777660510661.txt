Implement the canonical 55-theme universe migration for the shared thematic intelligence layer.

Context:
Backend audit confirmed the new /api/themes/relative-strength engine is canonical but isolated.

Current problem:
- ThematicContextProvider still reads old sr:theme_data:v2 and sr:dashboard:v1.
- DynamicThematicUniverse still uses old THEME_ETF_UNIVERSE.
- theme_ticker_mapper only knows 31 old themes and misses many new RS proxy symbols.
- TA Screener and Terminal inherit old thematic context from those shared layers.
- Home still uses old static THEME_MAP.
- Earnings should not be touched.

Goal:
Make the shared thematic intelligence layer consume the new 55-theme universe from theme_rs_service / THEME_RS_UNIVERSE.

Do not change frontend.
Do not change Earnings.
Do not change Watchlist.
Do not change Portfolio.
Do not change Chain Reaction.
Do not add LLM calls.

Step 1 — ThematicContextProvider
Update ThematicContextProvider so its primary source is the new canonical theme RS service:
- get_theme_rs_data(classification="all")
- or direct internal service call if cleaner

It should build:
- active_themes
- emerging_themes
- dead_zones
- sector_leaders
- sector_laggards
- macro_regime if currently available
- source_health

Use the new row fields:
- display_name
- classification
- parent_sector
- return_pct
- rs_score
- state
- state_reason
- proxy_symbols_used
- leaders
- laggards
- sector_tags

Mapping:
- state="active" → active_themes
- state="emerging" → emerging_themes
- state="dead_zone" → dead_zones
- classification="sector" rows → sector_leaders / sector_laggards
- classification="theme" and "sub_theme" rows → thematic lists

Keep existing LKG/fallback behavior.
Do not remove fallback yet, but demote old sr:theme_data:v2 to fallback only if new theme_rs_service fails.

Step 2 — theme_ticker_mapper
Update theme_ticker_mapper to index the new THEME_RS_UNIVERSE.

It should map:
- proxy_symbols
- proxy_symbols_used if available
- candidate/fallback symbols if present
- leaders/laggards symbols if available from cached RS snapshot
- aliases if available

Output should support:
- ticker → theme_name
- ticker → theme_id
- ticker → classification
- ticker → parent_sector
- ticker → theme_state if available
- regime_alignment_label / score as before

Keep old THEME_MAP / THEME_ETF_UNIVERSE as fallback only, not primary.

Step 3 — DynamicThematicUniverse
Update DynamicThematicUniverse to use the new THEME_RS_UNIVERSE instead of old sector_rotation.theme_universe.THEME_ETF_UNIVERSE.

Use new theme RS rows for:
- proxy ETF augmentation
- ETF holdings expansion
- FMP peer expansion
- cached X/Grok consensus mapping if present

Keep _KEYWORD_ETF_MAP only as supplemental fallback.

Step 4 — Terminal / TA Screener compatibility
Do not rewrite TA Screener.
Do not rewrite Terminal.

Confirm after Steps 1–3 that:
- TA Screener thematic fields now reflect the 55-theme universe
- Terminal context overlay now pulls active/emerging/dead themes from the new theme RS universe

Step 5 — Home page audit/follow-up only
Do not change Home in this pass unless it is trivial and low risk.
Report what Home still uses:
- static THEME_MAP
- sector_rotation.service.get_dashboard
- any old thematic source

Step 6 — Options Flow clarification
Audit says services/predict has no thematic overlay. Earlier work may have touched a different Options Flow endpoint.
Please clarify:
- Which Options Flow endpoint powers the current frontend page?
- Does that endpoint use DTU/theme_ticker_mapper?
- If not, report only. Do not implement yet.

Step 7 — Validation
Run:
- GET /api/thematic-context/snapshot
- GET /api/themes/relative-strength?classification=all
- TA Screener endpoint/path used by Terminal
- Terminal /api/query with a theme-sensitive prompt if safe

Confirm:
- ThematicContextProvider source_health shows new theme_rs_service as primary
- active_themes include names from the 55-theme universe
- sector_leaders come from classification=sector rows
- theme_ticker_mapper recognizes proxies from the new universe, including old preserved themes like QTUM, SRVR, BLOK, ARKX, SKYY, FCG, IHI, OIH
- no LLM calls added
- no Earnings changes
- no Chain Reaction changes
- no Watchlist/Portfolio changes
- no frontend changes

Delivery report:
1. Files changed
2. What old sources were demoted to fallback
3. Proof ThematicContextProvider now uses theme_rs_service
4. Proof theme_ticker_mapper indexes THEME_RS_UNIVERSE
5. Proof DTU uses THEME_RS_UNIVERSE
6. Validation output
7. Any remaining pages still using old static theme maps