FINAL FINAL PERFORMANCE CLOSURE — TERMINAL PREWARM

Authorize ONE additional production file:

backend/data/caelyn_terminal.py

This is now explicitly approved.

Preserve ALL currently validated uncommitted work in:

- backend/main.py
- backend/services/watchlist_rss_sweeper.py
- backend/tests/test_startup_scheduling.py
- any focused RSS test file already modified/added during this incident

Do NOT revert the startup-wave, master-screener, or RSS corrections.

Do NOT touch frontend.

Do NOT add:
- a new provider
- a new cache architecture
- a new scheduler
- a new history store
- a new worker system
- a new endpoint
- a new DB table
- a replacement Terminal pipeline

==================================================
PROVEN CURRENT STATE
==================================================

The previous pass established:

MASTER SCREENER
- real live cycle completed
- Stage 1 cached
- Stage 2 ~4.4s
- Polygon ~3.0s
- live scan ~8.3s
- 4 Tradier calls
- 60s recurring idle restored
- 10/10 Watchlist during master
- 0.65–0.99s

RSS
- full real sweep completed
- 490/490 CURRENT canonical Watchlist tickers
- 490 successful
- Yahoo 490 successful
- Google 490 successful
- 0 partial / 0 failed
- duration ~71.5s
- bounded ticker concurrency = 8
- archive write concurrency <=4
- minimum post-sweep idle = 60s
- 10/10 Watchlist during RSS
- all seven Home/backend endpoints HTTP 200

IMPORTANT:
490 is currently the canonical membership count.

Do NOT treat historical 489 as an invariant anymore.

At runtime validation:
- read canonical membership first
- record count + deterministic membership hash
- require Watchlist response to match THAT current count/hash

The remaining repeated provider signature is:

CaelynTerminal._fetch_tradier_histories()

during startup Terminal prewarm.

==================================================
REPOSITORY EVIDENCE
==================================================

Current caelyn_terminal.py:

_fetch_tradier_histories(...)

does essentially:

with lane("maintenance"):
    tasks = [
        self.tradier.get_history(sym, "daily", start)
        for sym in syms
    ]
    results = await asyncio.gather(*tasks, ...)

This launches the entire Terminal history universe concurrently.

TradierProvider ALREADY contains the correct existing background API:

get_history_background(...)

Its documented behavior is:

1. cache hit -> immediate
2. in-flight duplicate -> coalesce
3. maintenance lane budget check
4. non-blocking global capacity admission
5. preserve reserve for interactive traffic
6. physical HTTP only when admitted
7. defer immediately rather than sleeping behind the rate limiter

That method explicitly exists for schedulers / warmups / background history work.

Therefore:

DO NOT create another rate-control mechanism.

Use the existing provider architecture correctly.

==================================================
PART 1 — KEEP INTERACTIVE TERMINAL SEMANTICS UNCHANGED
==================================================

A real user request to the Terminal must preserve its current behavior.

Interactive cache-miss Terminal builds may continue using:

self.tradier.get_history(...)

with the existing response/fallback semantics.

Do NOT globally replace all Terminal history calls with the background variant.

Do NOT degrade an interactive Terminal request merely because it shares code with startup
prewarm.

Introduce the narrowest internal distinction necessary between:

A. interactive Terminal build
B. startup/background Terminal prewarm

This may be an optional internal parameter such as:

background_prewarm=False

or equivalent.

Default MUST preserve current interactive behavior.

No public endpoint contract change.

==================================================
PART 2 — TERMINAL PREWARM MUST USE EXISTING BACKGROUND ADMISSION
==================================================

When _terminal_prewarm() invokes CaelynTerminalProvider during startup, its Tradier historical
requests must use:

TradierProvider.get_history_background(...)

NOT the normal blocking/interactively admitted:

get_history(...)

Use the existing:

lane="maintenance"

and preserve meaningful interactive reserve.

Do not lower the global Tradier limit.
Do not raise the global Tradier limit.
Do not create a second limiter.

Also bound the number of Terminal background history coroutines actively performing work at
once.

Do not launch the entire history universe as an unconstrained gather.

Use a small local async semaphore in the existing method/background branch.

Preferred initial bound:

4 concurrent background history operations maximum

If live evidence demonstrates 4 is still disruptive, lower it.
Do NOT increase beyond 4 in this task.

==================================================
PART 3 — CRITICAL: DO NOT TURN DEFERRED TRADIER INTO A YAHOO STORM
==================================================

Current Terminal logic identifies missing Tradier histories and runs Yahoo history fallback.

That is appropriate for an INTERACTIVE build where Tradier genuinely cannot provide a ticker.

It is NOT appropriate when get_history_background() intentionally returned no data because:

- maintenance lane had no budget
- global reserve needed protection
- background request was intentionally deferred

A background admission deferral must NOT cause the startup prewarm to fan out equivalent
Yahoo history requests for every deferred ticker.

That would merely move the contention from Tradier to Yahoo/default executor.

Therefore background prewarm must distinguish:

- genuine unsupported/missing historical data
from
- intentionally deferred background history work

Use the narrowest internal mechanism possible.

DO NOT modify TradierProvider to add another status API unless absolutely necessary.

Prefer solving this inside caelyn_terminal.py using the known prewarm execution mode.

Acceptable prewarm behavior:

- consume already-cached histories immediately
- opportunistically fetch histories admitted through get_history_background()
- do not Yahoo-fallback histories merely because background admission deferred them
- do not block startup waiting for unavailable background capacity

Interactive Terminal behavior remains unchanged.

==================================================
PART 4 — DO NOT CACHE A MISLEADINGLY COMPLETE TERMINAL PAYLOAD
==================================================

Inspect the current Terminal caching behavior carefully.

If background prewarm is unable to obtain enough history because background admission was
deferred, do NOT write a normal 300-second Terminal cache entry that falsely appears fully
hydrated if doing so would suppress a proper interactive rebuild.

Choose the narrowest existing-compatible behavior.

Preferred:

- if prewarm produces the same complete payload as a normal build, cache normally
- if critical history was intentionally deferred, either:
  a. do not commit that incomplete result to the normal Terminal cache, OR
  b. use an already-existing partial/degraded freshness contract if one exists

Do NOT invent a second Terminal cache.

Do NOT change the frontend response contract.

A later genuine interactive request must still be able to obtain the full canonical Terminal
payload.

==================================================
PART 5 — AUDIT ONLY OTHER PREWARM FANOUT INSIDE THE SAME BUILD
==================================================

Do not broaden this into another service audit.

While editing caelyn_terminal.py, inspect only the concurrent task bundle inside _build():

- tradier_quotes
- crypto_quotes
- commodity_quotes
- tradier_history
- crypto_history
- commodity_history
- SPY history
- earnings
- news
- tape
- theme_mapping
- fundamentals

Do NOT change these paths automatically.

The runtime evidence names Tradier historical fanout as the blocker.

Only change another Terminal subtask if direct measurement during this final validation proves
it is independently causing the remaining ping/health starvation.

==================================================
PART 6 — TESTS
==================================================

Add focused regression coverage proving:

1. INTERACTIVE TERMINAL
   - uses normal get_history semantics
   - current fallback behavior remains unchanged

2. BACKGROUND PREWARM
   - uses get_history_background
   - does NOT use normal get_history for background history
   - maximum concurrent background histories <=4

3. HEADROOM
   - background admission deferral returns promptly
   - it does not wait behind the normal Tradier limiter

4. NO YAHOO DEFERRAL STORM
   - background-deferred Tradier histories do not automatically create Yahoo fallback requests
     for the same symbols

5. CACHE CORRECTNESS
   - a partial/deferred prewarm cannot poison the normal Terminal cache for 300 seconds
   - a later interactive request remains able to perform the complete build

6. CACHE HIT
   - existing Tradier history cache is reused normally

7. Existing Terminal response fields/contracts remain unchanged.

Also rerun:

- startup scheduling tests
- RSS tests
- Watchlist LKG tests
- Theme RS startup tests
- any focused Terminal tests already present

==================================================
PART 7 — FINAL CLEAN RUNTIME VALIDATION
==================================================

Restart with a verified fresh PID.

Do NOT generate heavy diagnostic polling that itself distorts results.

At process readiness establish:

- canonical Watchlist count
- canonical membership hash

Use those values for validation.
Do not hardcode 489 or 490.

Continuously probe:

GET /ping
GET /health

from server readiness through ALL of the following:

- early startup waves
- Terminal prewarm
- master screener first live cycle
- master 60-second idle
- RSS first full real sweep
- RSS post-sweep 60-second idle

TERMINAL PREWARM EVIDENCE

Report:

- number of portfolio/closed-trade/option-underlying symbols requiring history
- history cache hits
- background Tradier admissions
- background Tradier deferrals
- maximum Terminal history concurrency
- Yahoo history fallbacks caused by background deferrals — MUST BE ZERO
- total Terminal prewarm duration
- Tradier calls attributable to Terminal prewarm
- Tradier headroom before/during/after

PING / HEALTH ACCEPTANCE

- ZERO failed probes
- no multi-second event-loop starvation

WATCHLIST

Run 10 warm requests during Terminal prewarm itself.

Requirements:

- 10/10 HTTP 200
- current canonical membership count/hash
- zero fresh-LKG load_watchlist calls
- zero timeouts
- backend-induced TTFB <2s

Repeat 10 during RSS.
Repeat 10 during master.
Repeat 5 in a genuine idle interval >=60 seconds after RSS completion.

HOME MATRIX

During the heaviest active period test:

- Home dashboard
- Daily Alpha Board
- Risk Intelligence
- Trading Dashboard
- Theme RS
- Prediction Markets
- News

All HTTP 200.

The prior 15–16 second starvation must be gone.

POSTGRES

Report:

- held connection count
- exhaustion count at process start
- exhaustion count at finish
- top holders if exhaustion increases

Acceptance target:
no new exhaustion caused by the startup/background workload.

==================================================
PART 8 — GIT / COMMIT / PUSH
==================================================

Only after full acceptance passes.

Current remote source-of-truth commit before this closure remains:

1a46296cde913f826516545b52f68cad10ef82bb

Preserve all intended incident changes currently in working tree.

Final production files may now include EXACTLY:

- backend/main.py
- backend/services/watchlist_rss_sweeper.py
- backend/data/caelyn_terminal.py

Plus the already-used focused test files.

No fourth production file without stopping.

Before commit:

1. inspect local history for Replit auto-checkpoint noise
2. ensure local main is cleanly based on origin/main
3. preserve working tree while removing any unwanted local checkpoint from history
4. git diff --check
5. run all focused suites
6. stage ONLY approved source/test files
7. inspect staged diff
8. exclude runtime caches/snapshots/reports/memory/attachments/generated data

Create ONE clean performance-closure commit.

Push main.

Verify origin/main points to that exact clean commit.

DO NOT republish automatically.

==================================================
FINAL ACCEPTANCE
==================================================

End with:

READY_TO_REPUBLISH_BACKEND = YES

ONLY IF:

- all focused tests pass
- Terminal background history no longer uses blocking interactive admission
- no Yahoo fallback storm occurs from background deferrals
- ping failures = 0
- health failures = 0
- no new PostgreSQL exhaustion
- Watchlist passes during Terminal/RSS/master
- Home matrix passes without starvation
- real RSS sweep completed
- real master cycle completed
- Terminal prewarm actually executed
- clean commit pushed to origin/main

Otherwise:

READY_TO_REPUBLISH_BACKEND = NO

and name the exact remaining function.

This is the last production-file authorization for this incident.