CONTINUE THE SAME PERFORMANCE INCIDENT.

Do NOT treat the remaining latency items as independent optional follow-up tasks yet.

The broad site outage was correctly traced to the Theme RS startup six-way fanout:
after serialization, trivial endpoints recovered from 12-20s/timeouts to single-digit
milliseconds. That proves it was a shared outage mechanism.

However, the incident is NOT fully closed merely because /ping and /health recovered.

The user previously had a near-instant Watchlist.
After the recent Watchlist repair work it began loading again but became extremely slow.
Around the same degradation period, the rest of the site then became unavailable.

Therefore we need to distinguish:

A. the shared Theme RS starvation that caused the total backend outage
B. performance regressions introduced by the same recent repair/change window
C. genuinely pre-existing endpoint-specific latency

Do not assume B is C.

DO NOT MAKE ANOTHER EDIT YET.

PHASE 1 — ESTABLISH THE PERFORMANCE TIMELINE

Use git history, existing diagnostics/reports, runtime evidence, and tests to identify:

1. The last known Watchlist implementation where the Primary Watchlist loaded near-instantly.
2. The commits between that known-good state and the current slow state.
3. The performance-related commits from the recent Watchlist repair sequence, especially:

- cf19ba54a371d114a0217993e47ca0053e736af2
  Repair Watchlist cold-load reliability

- f0b41157034aa39976a7480333fd7ec52a29ab89
  Bound Watchlist detail latency

- b2837ee2f0d94fbe983a2f92b1895199cc03e454
  Use thread-safe PostgreSQL connection pool

- 44689bc9841f77526858aeb691640c170128ab40
  Remove redundant watchlist list DB preflight

- 1f5c847a2711f5b4742959dd905199709b7ca677
  Run watchlist rank snapshot reads concurrently

- 8ddcb353cbc4b27fe83dce21b5eb858e1183e393
  Bound watchlist rank snapshot reads

- 93d3b29c9b952ab875a5a581f3a63eb8d827b7ec
  Prevent startup jobs from blocking watchlist requests

Compare the CURRENT hot path to the historical fast path.

I want the first exact divergence that made the previously near-instant Primary Watchlist
materially slower.

Do not redesign the Watchlist.

PHASE 2 — CURRENT WATCHLIST STAGE TIMING

Instrument the existing Primary Watchlist response path and report milliseconds for EACH
existing stage.

At minimum:

- load saved Watchlist membership
- canonical Watchlist DB read
- quote cache lookup/hydration
- name overrides
- sector/category overrides
- fundamentals snapshots
- market-cap snapshots
- RV rank snapshot
- Vol/MC rank snapshot
- price-history/comparison tails
- theme/taxonomy merge
- earnings merge
- any options enrichment
- any analysis/skeleton construction
- serialization
- total endpoint time

Do this for:

1. first request after restart/warmup
2. immediate second request
3. third request

Report row count and response bytes each time.

If total response time is several seconds but no measured stage accounts for it, find the
missing wait rather than guessing.

PHASE 3 — CHECK FOR RECENT "PERFORMANCE FIX" REGRESSIONS

The recent fixes introduced additional threading/concurrency and timeout/bounding behavior.

Specifically determine whether any of these caused the new slow Watchlist:

- concurrently scheduling work that still must complete before response
- executor saturation
- unnecessary asyncio.gather waits
- timeout wrappers that wait for the full timeout repeatedly
- sequential timeout fallbacks
- DB calls moved to threads but still awaited as a group
- rank snapshot reads repeated per request
- cache-first paths still doing expensive enrichment before returning
- skeleton response built only after slow optional dependencies
- large response serialization
- repeated disk/cache reads that were formerly memoized
- thread-pool contention with background jobs
- hidden per-ticker loops
- historical-data reads accidentally returned to the hot path

Trace actual runtime, not just static code.

PHASE 4 — HOME "RESIDUAL LATENCY"

Do the same classification for the Home endpoints DeepSeek described as still having
endpoint-specific latency.

For each slow Home endpoint answer:

1. What is its current warm latency?
2. What was its latency before this incident if historical evidence exists?
3. Did any commit in the same recent change window alter its path or shared dependency?
4. Does it become fast when background work is fully idle?
5. Does it become slow only during a particular existing background job?

We are trying to determine whether there is still ONE shared contention source after the
Theme RS fix, not prematurely turn every symptom into its own optimization project.

PHASE 5 — BACKGROUND RESOURCE OVERLAP

The Theme RS fanout was one shared starvation source.

Now measure whether a single remaining shared resource is still slowing the expensive
endpoints even though trivial endpoints survive:

- default executor occupancy / queue depth
- CPU saturation
- disk I/O
- active yfinance/history work
- active FMP fallback work
- PostgreSQL pool state
- number of concurrent background jobs
- GIL-heavy parsing/scoring

A system can have /ping at 7ms while expensive endpoints are still delayed by executor,
CPU, or shared-cache contention.

Prove or reject that.

IMPORTANT:

Do not say "Confluence is in a background thread, therefore it cannot be contributing."

A background thread can still consume:
- CPU
- GIL time
- executor capacity
- disk bandwidth
- database connections
- memory bandwidth
- provider limits

Measure whether it affects request latency.

The reported 554-symbol Confluence build taking 77.2 seconds deserves measurement, even
if it is not directly blocking the asyncio event loop.

PHASE 6 — CLASSIFY BEFORE FIXING

At the end of the audit, classify every residual slowdown as exactly one of:

A. SAME INCIDENT — shared contention/regression from recent changes
B. WATCHLIST REGRESSION — introduced while repairing the Watchlist
C. PROVEN PRE-EXISTING — demonstrated to exist before this incident
D. UNKNOWN — insufficient evidence

Do not label something "endpoint-specific follow-up" unless it is category C.

If A or B is proven, continue this SAME incident and propose the narrow correction.

If more than two production files are required, stop and report first per AGENTS.md.

SUCCESS CONDITION

The actual desired state is not merely:

- server no longer crashes
- /ping is fast
- Watchlist eventually loads

The desired state is restoration of the known-good product behavior:

- site remains responsive during startup/background activity
- Home modules populate normally
- Primary Watchlist returns near-instantly from existing cached/canonical data as it did
  before the regression
- no external provider fanout in interactive request paths
- no architectural replacement

Update /.opencode-reports/latest.md with this continuation investigation.

Do not split this into Replit's suggested tasks.
This remains one regression investigation until the timeline proves otherwise.