Workflow rules:

Work directly on main
Do NOT create a branch
Do NOT create a PR
Commit directly to main
Push directly to main

This is a focused backend bug/feature fix. Do not broaden scope.

CRITICAL CLARIFICATION
The existing X section is already working and must NOT be broken or replaced.

Keep these exactly as they are:

Home page: Trending on X
Social page: the existing first section (same underlying data / same working path; just labeled for Social)

Do NOT change:

the existing consensus/trending X payload currently powering that working section
its refresh behavior
its rendering contract
its cached read behavior

PRIMARY GOAL
Add the three new Social-only sections alongside the already-working existing X consensus section, without changing the existing section’s backend path/shape.

The 3 new sections needed are:

freshest_alpha
theme_leadership
sentiment_acceleration

So the Social page should effectively have:

existing working X consensus section
plus 3 additional derived sections

ROOT PROBLEM
Right now the Social page is still only receiving the old working X snapshot subset shape, which is fine for the existing consensus section — but the 3 new sections are not being provided.

So do NOT replace the old working shape.
Instead, extend the Social dashboard/backend response to include the 3 additional sections while preserving the existing working section exactly.

AUDIT FIRST
Inspect and prove:

What exact backend payload currently powers:
Home Trending on X
Social existing consensus section
Confirm that payload remains untouched and working
Identify the safest place to extend the Social-only response with:
freshest_alpha
theme_leadership
sentiment_acceleration
without changing the existing consensus payload contract

IMPLEMENTATION REQUIREMENT
Build or fix the Social dashboard response so it includes:

A. The existing working consensus/trending payload unchanged

same fields
same shape
same cached behavior

B. Additional Social-only sections appended alongside it:

freshest_alpha
theme_leadership
sentiment_acceleration

These 3 new sections must be derived from the existing cached X snapshot and prior snapshot history.
No additional Grok/XAI calls.

PREFERRED RESPONSE SHAPE
Keep the existing working section under its current key/path exactly as-is.
Then add new sibling keys for the 3 new sections.

Example conceptually:
{
...existing_working_consensus_payload_unchanged...,
"freshest_alpha": {...},
"theme_leadership": {...},
"sentiment_acceleration": [...],
"market_pulse": {...},
"portfolio_bias": "...",
"metadata": {...}
}

Important:

do NOT rename or reshape the existing working consensus payload
only add the new fields cleanly

SECTION SOURCES

freshest_alpha -> from raw.fresh_trades + spotlight freshest_alpha
theme_leadership -> from raw.hype_radar + market pulse context
sentiment_acceleration -> derived from current vs prior snapshot
zero extra Grok calls

VERIFY
Before finishing, prove:

Home Trending on X payload remains unchanged
Social existing consensus section payload remains unchanged
Social response now additionally includes:
freshest_alpha
theme_leadership
sentiment_acceleration
no extra Grok/XAI calls were introduced
prior snapshot delta is used only for sentiment acceleration

DELIVERABLES
When done, report:

exact backend files changed
exact existing payload path kept unchanged
exact new fields added for Social only
exact live payload keys after the fix
confirmation the original working consensus section was not changed
confirmation no extra Grok/XAI calls were introduced
confirmation changes were committed and pushed directly to main

EXECUTION STYLE

Preserve the working section exactly
Add the 3 new sections next to it
Do not “upgrade” the existing section into something else
Then commit and push directly to main