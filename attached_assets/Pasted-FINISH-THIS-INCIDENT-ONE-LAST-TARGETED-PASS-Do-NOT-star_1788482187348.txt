FINISH THIS INCIDENT. ONE LAST TARGETED PASS.

Do NOT start another architecture investigation.
Do NOT edit another service implementation.
Do NOT add another production file.
Do NOT touch frontend.
Do NOT revisit startup orchestration.

The DB startup fix is accepted:

- live Neon schema_current=true
- catalog check ~80–100 ms
- full DDL skipped
- deferred startup completed in 19.042s
- immediate /ping 20/20
- immediate /health 20/20
- health no longer launches schema mutation

The only unresolved issue is the TRANSIENT POST-STARTUP stall that occurred AFTER
_deferred_sync_startup completed.

TASK
====

Use the EXISTING local workflow log from the failed soak.

Do not restart yet.

Find the exact timestamps of:

1. the first Watchlist discovery request that timed out at 30s
2. the three consecutive /ping timeouts
3. the three consecutive /health timeouts

For that exact window, print ONLY:

- every background task/job active at that moment
- exact function name
- launch site in backend/main.py
- whether it was:
  a) restart/catch-up work
  b) a normal recurring scheduled job
  c) request-driven work
- its start time
- its end time if known
- provider calls / DB activity associated with it

Do not speculate from nearby timestamps.

Name the ONE job whose active window actually spans the failures.

==================================================
IF IT IS AUTOMATIC RESTART / CATCH-UP WORK
==================================================

Disable ONLY its automatic restart registration in backend/main.py.

Do not edit its service.

Preserve:
- on-demand path
- persisted cache/LKG
- normal future wall-clock scheduler if one exists
- provider/business logic

Process restart must not trigger it.

==================================================
IF IT IS A RECURRING LOOP THAT FIRES IMMEDIATELY
==================================================

Change ONLY its registration/start behavior in backend/main.py so the web process does not
perform an immediate heavy first pass after restart.

Do not change its service internals.

If it has a natural recurring interval or wall-clock schedule, allow its FIRST automatic run
to occur at that normal future time rather than immediately after boot.

==================================================
IF IT IS REQUEST-DRIVEN
==================================================

STOP.

Name the exact request function and do not change code automatically.

==================================================
FINAL VALIDATION
==================================================

After the one main.py registration correction, restart ONCE.

Do not wait minutes before testing.

After HTTP readiness:

20 x /ping
20 x /health

Then after deferred startup completes:

10 x Primary Watchlist
- discover canonical membership dynamically
- 10/10 HTTP 200
- no timeout

Home matrix:
- Trading Dashboard
- Daily Alpha
- Risk Intelligence
- Theme RS using valid 30D parameter
- Prediction Markets
- News

All HTTP 200.

Then leave the backend running for 10 minutes with lightweight:

- /ping every ~10s
- /health every ~10s

No multi-megabyte polling.

Acceptance:
- zero ping timeouts
- zero health timeouts
- zero Watchlist timeout
- no PostgreSQL exhaustion increase
- no provider-heavy restart catch-up
- deferred startup remains fast

==================================================
COMMIT IT
==================================================

If that passes, STOP TESTING.

Final production files may be ONLY:

- backend/main.py
- backend/services/watchlist_rss_sweeper.py
- backend/data/caelyn_terminal.py
- backend/data/pg_storage.py

No fifth production file.

Run:
- focused tests
- git diff --check
- inspect full diff

Remove abandoned coordinator tests/code completely.

Stage only approved source + focused test files.
No caches, snapshots, runtime files, reports, memory, attachments.

Create ONE commit:

perf: restore responsive backend startup and background headroom

Push main.
Verify origin/main is the new commit.

Do NOT republish automatically.

End with:

READY_TO_REPUBLISH_BACKEND = YES

If the 10-minute validation still fails, do not investigate anything else.
Report the exact job/request that spans the failure and:

READY_TO_REPUBLISH_BACKEND = NO