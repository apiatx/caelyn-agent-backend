You are in the CaelynAI backend Replit project.

The Portfolio Review button now returns:

Failed to get portfolio review. Please try again.
Server returned 500: {"error":"Agent returned 500"}

This means the frontend is reaching the backend endpoint, but the backend portfolio review agent/synthesis call is failing internally.

Do NOT change the frontend yet.
Do NOT create a new endpoint.
Do NOT rebuild the Portfolio page.
Fix the backend Portfolio Review endpoint so it never returns a generic “Agent returned 500” when portfolio data exists.

Tasks:

1. Find the exact Portfolio Review endpoint currently returning:
   {"error":"Agent returned 500"}

2. Trace the internal call stack:
   - route handler
   - portfolio review service
   - agent/LLM call
   - prompt/context builder
   - any TradingAgent/query_agent/Claude/Anthropic wrapper being used

3. Identify the real exception.
   Add temporary server-side logging around the failing agent call:
   - exception type
   - exception message
   - response/status from model provider if available
   - whether context payload is empty/too large/malformed
   - which model/provider is being called
   - whether API key/env var is missing

4. Fix the root cause, not the symptom.

Common likely causes to check:
   - wrong agent function signature
   - missing required field like query/message/user_id/session_id
   - passing a dict where the agent expects a string
   - malformed portfolio context
   - prompt too large
   - non-JSON-serializable data in context
   - Anthropic/OpenAI/Gemini wrapper raising but being swallowed
   - model response parser expecting JSON but receiving plain text
   - missing AUTH/user context
   - endpoint calling the generic chat agent incorrectly
   - provider env var mismatch: ANTHROPIC_API_KEY, OPENAI_API_KEY, XAI_API_KEY, GEMINI_API_KEY

5. Add a deterministic fallback.
   If the LLM/agent call fails, the endpoint must still return HTTP 200 with a useful portfolio review generated from existing Portfolio Terminal metrics.

Fallback response should include:
   - portfolio_summary
   - risk_flags
   - weighting_suggestions
   - asset_reviews
   - raw_inputs_used
   - agent_status: "fallback"
   - agent_error_summary: short internal-safe reason like "LLM synthesis unavailable"

Do not expose stack traces to the frontend.

6. Update the endpoint behavior:
   - If portfolio holdings exist, return 200 even if the LLM fails.
   - Only return 4xx/5xx if there are no holdings or the route itself cannot load portfolio data.
   - Partial unavailable sources like options flow/news/social/earnings should not fail the review.

7. Make the agent call safer:
   - Build a compact portfolio review context instead of dumping the entire Portfolio Terminal payload.
   - Cap holdings reviewed if necessary, but prioritize largest weights and highest-risk names.
   - Strip circular/non-serializable objects.
   - Convert dates/Decimals/NaN/Infinity safely before JSON serialization.
   - Use existing cached Portfolio Terminal/page data wherever possible.

8. Validation:
   Run the exact endpoint hit by the Portfolio Review button.

Confirm:
   - HTTP 200
   - no {"error":"Agent returned 500"}
   - response includes either agent_status: "ok" or agent_status: "fallback"
   - response includes portfolio_summary, weighting_suggestions, asset_reviews, risk_flags
   - backend logs show the real root cause if the LLM call failed
   - frontend Portfolio Review panel can render the response

Return:
   - root cause of the 500
   - files changed
   - whether the live LLM agent works or fallback triggered
   - exact curl command used
   - trimmed sample response