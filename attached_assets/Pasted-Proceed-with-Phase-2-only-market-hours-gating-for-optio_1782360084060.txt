Proceed with Phase 2 only: market-hours gating for options-heavy Tradier background loops.

Use the Phase 1 audit/results. Do not build active page priority yet. Do not build a new scheduler. Do not create new cache namespaces. Preserve existing endpoint response shapes.

Goal:
Stop options-chain background loops from running at near-full speed 24/7. Options-chain work should run normally during pre-market, regular market, and post-market Monday–Friday, and slow down overnight/weekends.

Target loops from the audit:

1. `_master_screener_loop` in `main.py`

   * currently ~34s cycle + 5s sleep / near-continuous
   * uses Tradier options expirations + chains
   * currently no market-hours gate

2. `_theme_options_supplement_loop`

   * currently every 5 minutes, 20 symbols/run
   * uses Tradier options expirations + chains
   * currently no explicit market-hours gate

3. Any other options-chain background loop discovered while editing

   * include only if it is clearly options-chain heavy
   * do not touch quote-only paths unless needed for diagnostics

Market session policy:
Use Eastern Time.

Define:

* premarket: 04:00–09:30 ET
* regular: 09:30–16:00 ET
* postmarket: 16:00–20:00 ET
* off_hours: 20:00–04:00 ET
* weekend: Saturday/Sunday

Desired behavior:

1. During premarket/regular/postmarket:

   * options loops run normally, but still through existing limiter/coalescing/in-flight guard.
   * no rate-limit increase.
   * no 429s.

2. During off_hours/weekends:

   * drastically slow options-chain loops.
   * no near-full-speed master screener loop.
   * no constant supplement scanning.
   * allow only low-rate maintenance if needed, such as:

     * stale LKG cleanup
     * very small retry batches
     * cached diagnostics
   * suggested sleep/cadence:

     * master screener: every 10–30 minutes, or disabled except maintenance
     * supplement loop: every 30–60 minutes, or disabled except maintenance

3. Page endpoints:

   * should still return cached/last-good data immediately.
   * should not blank rows because the background loop is slowed.
   * user-triggered/manual refresh can still enqueue work, but must respect limiter.

4. Options Flow:

   * preserve existing Options Flow screener response shape.
   * do not break the Options Flow page.
   * during active sessions, Options Flow should continue to update.
   * off-hours can serve LKG/stale status instead of hammering Tradier.

5. Watchlist/Portfolio/Themes/Sectors:

   * should keep reading shared cache/LKG.
   * failed/stale refresh must not hide last-good data.
   * Sectors remains aggregation-only for watchlist-overlap symbols.
   * `watchlist_overlap_live_calls` must remain 0.

Diagnostics required:
Add/extend status endpoint with:

* `tradier_market_session`: premarket | regular | postmarket | off_hours | weekend
* `is_tradier_active_session`
* `master_screener_loop_mode`: active | maintenance | sleeping
* `master_screener_next_run_at`
* `master_screener_last_run_at`
* `theme_supplement_loop_mode`
* `theme_supplement_next_run_at`
* `theme_supplement_last_run_at`
* `offhours_options_calls_suppressed_count`
* `options_loop_calls_last_60s`
* `calls_used_last_60s`
* `last_429_at`

Validation:

1. At current time, confirm the session classification is correct.
2. Temporarily simulate or unit-test each session state if possible:

   * premarket
   * regular
   * postmarket
   * off_hours
   * weekend
3. Confirm active sessions run loops normally.
4. Confirm off_hours/weekend slows or suppresses options-heavy loops.
5. Confirm endpoints still serve cached/LKG data:

   * Options Flow
   * Watchlist options
   * Options Flow Sectors
6. Confirm no Tradier 429s.
7. Confirm no raw unmanaged Tradier call paths were reintroduced.

Hard constraints:

* Do not raise Tradier RPM.
* Do not build active page priority.
* Do not build unified ticker registry yet.
* Do not change frontend contracts.
* Do not change Options Flow/Watchlist/Portfolio response shapes.
* Do not make Sectors live-scan watchlist-overlap symbols.

Final report:

* files changed
* exact loop cadence before/after
* session classification logic
* diagnostics added
* validation results
* remaining Phase 3 recommendation: budget/reserved capacity, not active page priority yet
