Add one narrowly scoped requirement to the current Watchlist performance work:

CSV data is bootstrap-only and must never function as an ongoing Watchlist data source.

Architectural contract:

- The authoritative Watchlist membership is the existing database-backed ticker membership path.
- CSV import exists only to identify symbols that should be added to that membership.
- CSV values may temporarily fill visible fields only while the first canonical FMP/Neon refresh for that imported ticker is still incomplete.
- Once canonical data exists for a ticker, CSV values must not override it, merge back into it, or remain as a fallback.
- Raw CSV rows, CSV blobs, and uploaded CSV files should not remain part of normal Watchlist reads after successful hydration.
- `csv_data` must not contain canonical nested objects such as:
  - earnings_intelligence
  - earnings history
  - live earnings events
  - options data
  - news
  - technical/confluence payloads
  - complete fundamentals snapshots
- Ticker-detail-only objects must never be duplicated inside bulk CSV rows.

Before editing, trace the existing CSV lifecycle:

1. CSV upload/parser.
2. Symbol extraction.
3. Watchlist membership writer.
4. Temporary CSV persistence.
5. First FMP refresh trigger.
6. Canonical Neon/cache write.
7. Bulk Watchlist reader.
8. Conditions under which CSV placeholders stop being used.
9. Whether the raw uploaded CSV file or parsed rows are ever deleted.
10. Whether CSV data can currently override newer FMP/Neon values.

Do not replace the authoritative Watchlist membership path.

Do not add another source of truth, table, endpoint, cache, scheduler, worker, or provider call.

Required behavior:

- On import, normalize and deduplicate symbols.
- Add the symbols through the existing authoritative Watchlist membership writer.
- Keep only the minimum temporary per-symbol placeholder fields needed for display while canonical hydration is pending.
- Canonical FMP/Neon fields always take precedence over temporary CSV fields.
- Clear a ticker’s temporary CSV fields as soon as its first canonical refresh succeeds.
- On partial refresh failure, retain temporary data only for the unresolved ticker or unresolved fields—not the full original CSV payload.
- Once all imported tickers are hydrated, delete the parsed temporary CSV data through the existing storage path.
- Do not delete the tickers from the Watchlist.
- Do not delete canonical Neon data.
- Do not perform a broad database cleanup without first reporting the exact rows and predicate.
- Existing old CSV-backed records must not be mass-deleted without approval. First report their count, size, and safe cleanup predicate.

Bulk Watchlist response contract:

- The normal Watchlist endpoint must not return raw imported CSV blobs.
- It may continue using the existing `csv_data` response key if the frontend contract requires it, but rows must be minimal canonical display rows—not persisted copies of the original CSV.
- No `earnings_intelligence` may appear inside bulk `csv_data`.
- No large ticker-detail object may be duplicated per ticker.
- Existing ticker-detail endpoints must continue returning their full data.

Validate:

1. Import a small CSV with three symbols and temporary placeholder values.
2. Confirm the three symbols are added through authoritative Watchlist membership.
3. Before canonical refresh, placeholders may display.
4. After canonical refresh for one symbol, that symbol uses only canonical values and its temporary CSV data is cleared.
5. Unresolved symbols may retain only their own temporary placeholders.
6. After all three refresh successfully, no parsed CSV payload remains.
7. Restart the application and confirm the tickers remain in the Watchlist through database membership.
8. Confirm no CSV fallback recreates membership.
9. Confirm a newer FMP value cannot be overwritten by an older CSV value.
10. Confirm bulk Watchlist response contains zero `earnings_intelligence` keys inside `csv_data`.
11. Report response bytes before and after.
12. Confirm ticker-detail Earnings and About data remain intact.

First provide the audit findings and proposed exact files before making any broad persistent-data cleanup.

Keep the immediate performance fix intact:
- bulk fundamentals must continue excluding `earnings_intelligence`
- ticker detail must continue receiving the full `earnings_intelligence` object
- GLW Q2 results must remain correct