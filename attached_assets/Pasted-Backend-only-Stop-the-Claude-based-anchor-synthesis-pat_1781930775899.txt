Backend only. Stop the Claude-based anchor synthesis path and replace it with OpenAI Responses API web-search research.

Do not run OPENAI or ANTHROPIC yet.

Current issue:
The current implementation uses Anthropic/Claude directly with no web search, no source retrieval, no source validation, and no real grounding. That is not acceptable for Chain Reaction / Bottlenecks anchor research.

New requirement:
Use OpenAI API / ChatGPT-style web search for this feature.

Implementation target:
Use the OpenAI Responses API with hosted `web_search`, not Anthropic.

Critical constraints:

* Do NOT use Claude/Anthropic for anchor supply-chain research.
* Do NOT use ungrounded LLM synthesis.
* Do NOT include private companies in researched output.
* Do NOT store private-company context rows for this feature.
* Do NOT ask for private companies.
* Do NOT allow private companies into the overlay table.
* Do NOT run research on page load.
* Do NOT run research during weekly Chain Reaction scoring.
* Do NOT change `/api/bottlenecks/current` response shape.
* Do NOT change weekly deterministic scoring.
* Do NOT redesign the frontend.
* Do NOT mutate existing `NODE_REGISTRY`.

User-facing product expectation:
For every researched result, the app should show:

* public ticker
* public company name
* existing TradingView chart for that ticker
* supply-chain writeup / detail drawer in the same Chain Reaction style
* evidence-backed relationship to the anchor

Therefore the research output should contain ONLY public, currently tradeable companies.

Required architecture changes:

1. Remove/disable Claude for this feature

* Replace `anchor_research_service.py` Anthropic client usage with OpenAI client usage.
* Rename service terminology from `synthesis` back to `research` only after it is actually grounded through web search.
* Remove `llm_only_no_grounding` as the production path.
* Keep old Claude-generated SPCX rows out of approved scoring until re-run through the OpenAI web-search path.

2. Use OpenAI Responses API with web search

* Use `OPENAI_API_KEY`.
* Use the OpenAI Responses API.
* Use hosted web search with `tools: [{"type": "web_search"}]`.
* Force web search to run; do not allow the model to skip search.
* Include returned source/citation metadata in the service response and stored rows.
* Use a fast web-search-capable OpenAI model configured by env var, for example:
  `OPENAI_ANCHOR_RESEARCH_MODEL`
* Default should be a current fast OpenAI model that supports Responses API web search.
* Do not hardcode deprecated preview search models.

3. Public companies only
   The prompt must ask:
   “Return only currently public, actively tradeable companies with valid ticker symbols. Do not include private companies. Do not include acquired/delisted/stale tickers. Do not include companies where the ticker cannot be verified.”

Remove these fields or make them constant:

* `is_public` should always be true for approved rows.
* No private map-context rows.
* No `CONFIRM_TICKER`.
* No blank ticker rows.
* No OTC/foreign names unless the existing app/TradingView supports that symbol cleanly and ticker validation passes.

4. Ticker validation gate
   Before a row can become `approved`, validate:

* ticker exists
* company is currently public/tradeable
* not delisted/acquired/stale
* TradingView-compatible symbol can be rendered
* preferably validated through existing FMP/Tradier/profile/reference cache paths

Reject or pending-review anything that fails.

Hard rule:
A node is not visible in Bottlenecks unless it has a current validated public ticker.

5. Source/evidence validation gate
   For every approved row require:

* at least one source URL from OpenAI web-search citations/sources
* at least one evidence string tied to that source
* no homepage-only generic source unless the source actually supports the specific claim
* no model-invented source URLs
* no evidence claim without a source URL

If the OpenAI response includes citations/annotations/sources, persist those. The stored source URLs must come from the web-search result metadata, not from model free-text alone.

6. Replace / quarantine old SPCX rows
   Current SPCX rows were generated by Claude-only ungrounded synthesis.
   Before continuing:

* mark existing SPCX rows from the Claude path as `pending_review`, `rejected`, or delete them from approved scoring
* do not allow them to appear as approved rows
* re-run SPCX using OpenAI web-search research
* only approve rows that pass public ticker validation and source validation

7. Update prompt
   Rewrite the canonical prompt so it asks for public-company bottlenecks only.

Prompt intent:
Use the Serenity bottleneck strategy, but only return public investable companies in the anchor’s supply chain.

The prompt should say:

* Research the named anchor’s supply chain using web search.
* Work backward from the anchor/end customer.
* Identify public companies that supply, enable, constrain, or are public-market proxies for the anchor’s physical/infrastructure supply chain.
* Return only current public companies with valid tickers.
* Exclude private companies entirely.
* Exclude unverified companies.
* Exclude broad generic AI/space/defense filler.
* Prefer scarce bottlenecks, undercovered suppliers, specialized tooling/components/materials/infrastructure, and companies where the supply-chain role is specific and explainable.
* Shared suppliers between OpenAI and Anthropic are allowed only when evidence supports the relationship separately for each anchor.

8. Structured JSON output
   Each row should include:

* anchor_key
* anchor_name
* ticker
* company_name
* exchange
* tradingview_symbol if known
* supply_chain_role
* relationship_type: direct / indirect / infrastructure / public_proxy
* themes
* layer
* bottleneck_score
* confidence
* evidence
* source_urls
* source_titles if available
* giant_anchors, containing the exact anchor key
* why_it_matters
* why_hidden
* why_now
* what_would_break_thesis
* public_market_proxy_reason
* last_researched_at

Remove from approved output:

* private/public status ambiguity
* private companies
* unresolved tickers
* `CONFIRM_TICKER`
* rows without sources
* rows without evidence
* rows that cannot render a TradingView chart

9. Monthly cadence
   Keep the monthly cadence:

* one OpenAI web-search research call per stale/missing anchor
* one anchor at a time
* no parallel calls
* no weekly LLM calls
* weekly job uses cached approved public rows only
* stale rows remain visible until replaced, but should be marked due for refresh

10. Multi-anchor endpoint

* Filter by exact `giant_anchors`, not theme overlap.
* NVDA uses `NVDA`.
* TSM uses `TSM`.
* GOOG maps to `GOOGL`.
* SPCX uses `SPCX`.
* OPENAI uses `OPENAI`.
* ANTHROPIC uses `ANTHROPIC`.
* If no approved public researched rows exist, return `needs_research`.
* Do not return generic theme fallback rows.

11. Validation order
    Do not run all anchors at once.

First:

* quarantine/delete old Claude SPCX approved rows
* re-run SPCX with OpenAI web search
* validate the results manually

Report:

* raw OpenAI route response summary
* model used
* number of web searches performed if available
* number of rows returned
* number approved
* number rejected
* rejection reasons
* 10 sample approved rows
* each sample row’s ticker, company, role, evidence, source URL, and TradingView symbol
* proof every approved row is public/current/tradeable
* proof every approved row has `giant_anchors` containing `SPCX`
* proof `/api/bottlenecks/multi-anchor` returns SPCX success from approved OpenAI-researched rows
* proof weekly scoring still has zero LLM calls
* proof page-load endpoints still have zero LLM calls

Only after SPCX is clean:

* run OPENAI
* validate
* then run ANTHROPIC
* validate

12. Frontend expectation
    No frontend work yet, but backend response must preserve enough fields so the existing frontend can show:

* public ticker
* public company name
* TradingView chart
* supply-chain detail drawer/writeup
* evidence/source links

Final deliverable:
Implement the OpenAI web-search replacement and report:

* files changed
* old Claude path removed/disabled
* prompt file path
* final prompt text
* OpenAI model/env var used
* DB/schema changes if any
* SPCX re-run results
* validation proof
