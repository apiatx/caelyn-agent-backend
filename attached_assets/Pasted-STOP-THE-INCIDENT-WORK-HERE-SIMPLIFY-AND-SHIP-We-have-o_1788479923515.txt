STOP THE INCIDENT WORK HERE. SIMPLIFY AND SHIP.

We have over-expanded this incident.

Do NOT optimize briefing.
Do NOT add another service file.
Do NOT continue the completion-aware startup coordinator.
Do NOT add another run_once() abstraction.
Do NOT perform another 30-minute forensic validation pass.

The latest experiment proved the central issue:

A provider-heavy background precompute can freeze the single web process even when it runs
completely alone.

Therefore serializing every heavyweight startup pass inside the web process is NOT the solution.

PRODUCT PRIORITY
================

Restore a fast, usable backend.

The API process must:

1. boot
2. load persisted/canonical/cache state
3. serve requests
4. NOT perform massive provider-heavy catch-up work merely because the process restarted

We can separately revisit how heavyweight maintenance is executed later.

RIGHT NOW, remove startup catch-up from the web-server critical path.

==================================================
STEP 1 — ABANDON THE LARGE ORCHESTRATION EXPERIMENT
==================================================

HEAD and origin/main are both:

1a46296cde913f826516545b52f68cad10ef82bb

The working tree currently contains a large uncommitted completion-aware orchestration
experiment across many service files.

DO NOT COMMIT THAT ARCHITECTURE.

First inventory the current working-tree diff and separate it into:

A. independently proven small performance fixes
B. experimental completion-aware coordinator / run_once / skip-initial plumbing added only
   for the abandoned orchestration design

Revert category B from the working tree.

Preserve runtime/generated files untouched.

Do not rewrite origin/main.

==================================================
STEP 2 — KEEP ONLY THE SMALL FIXES THAT WERE ACTUALLY PROVEN
==================================================

Preserve these only if their diff is still narrow and independently testable:

1. MASTER OPTIONS SCREENER
   - recurring idle restored from 5s to 60s
   - synchronous import/enrichment/disk persistence offloaded
   - provider/session behavior unchanged

This was live-proven:
- real cycle completed
- Stage 2 ~4.4s
- Polygon ~3.0s
- only 4 Tradier calls
- 60-second idle observed
- Watchlist during master 10/10 and sub-second

2. RSS SWEEPER
   - synchronous membership reads off event loop
   - ticker concurrency 15 -> 8
   - archive DB-write concurrency <=4
   - minimum 60-second idle after full sweep

This was live-proven:
- full 490/490 sweep
- zero provider failures
- ~57-71s completion
- Watchlist 10/10 during sweep

3. WATCHLIST LKG-FIRST

Already exists on origin/main as 1a46296c.
Do not touch it.

4. THEME RS SERIALIZATION

Already exists on origin/main.
Do not reopen it.

Anything else from the large startup coordinator experiment should be presumed unnecessary
unless it is required for the simplified startup rule below.

==================================================
STEP 3 — NO HEAVY STARTUP CATCH-UP IN THE WEB PROCESS
==================================================

This is the actual correction.

Audit backend/main.py startup registrations only.

At process restart:

KEEP:
- provider/data-service construction
- router registration
- cheap persisted LKG/cache hydration
- required DB/schema initialization
- lightweight canonical state hydration

DO NOT RUN AS STARTUP CATCH-UP:
- briefing precompute
- Terminal prewarm
- Calendar stale catch-up refresh
- Trading Dashboard startup nudge
- Confluence rebuild
- full RSS catch-up solely because of restart
- full Theme RS historical rebuild solely because of restart
- Whale/SEC catch-up
- insider/congressional catch-up
- Bittensor catch-up
- Polygon multi-hour ingestion catch-up
- heavy earnings catch-up
- heavy options backfill
- any other provider-heavy full-universe precompute whose only reason to run is:
  "the process restarted"

Persisted cache/LKG should be served after restart.

Do NOT delete the underlying jobs.

Do NOT change their endpoint contracts.

Do NOT change their providers.

Do NOT change their normal explicit/manual or true scheduled behavior.

The rule is only:

PROCESS RESTART != REQUIREMENT TO RECOMPUTE EVERYTHING.

If an existing recurring scheduler naturally has a future wall-clock run, register it so it
waits for that normal future run.

If an existing loop performs an immediate heavy first pass simply because it was started,
either:

- use an ALREADY-IMPLEMENTED narrow skip-initial control from the current working tree if it
  is clean and trivial

OR

- do not register that startup catch-up path from the web process.

Do NOT retain run_once() extraction machinery merely to make startup execute the heavy pass.
We specifically do not want the heavy startup pass anymore.

==================================================
STEP 4 — BRIEFING SPECIFICALLY
==================================================

Do not optimize _briefing_precompute_run_once().

Do not run briefing precompute during process startup.

The isolated validation proved it can consume >207 seconds and make /ping and /health
unresponsive even with no competing Class B job.

That is sufficient evidence to remove it from STARTUP.

Preserve any:
- on-demand briefing path
- persisted briefing LKG/cache
- normal scheduled refresh outside startup

If the ONLY way briefing ever refreshes today is this startup precompute, report that fact.
Do not redesign it during this incident.

==================================================
STEP 5 — VALIDATION SHOULD BE SHORT AND PRODUCT-FOCUSED
==================================================

We are no longer trying to prove that every maintenance job in the entire application can run
inside the web process without one missed 3-second synthetic probe.

Restart to a fresh PID.

Wait for required hydration to complete.

Then validate the actual product while the backend remains running for 10 minutes.

Required:

/ping
- 20 consecutive requests
- zero failures

/health
- 20 consecutive requests
- zero failures

Primary Watchlist
- discover canonical membership count at runtime
- 10 consecutive requests
- 10/10 HTTP 200
- correct membership
- no timeout
- warm requests should be near-instant server-side

Home:
- trading dashboard
- Daily Alpha
- Risk Intelligence
- Theme RS
- Prediction Markets
- News

All return HTTP 200.

Also open/check the major backend endpoints used by:
- Watchlist
- Home

No 30s/90s/180s stalls.

Record:
- CPU
- threads
- PostgreSQL pool exhaustion
- Tradier usage

Acceptance:
- no provider-heavy restart catch-up is running
- no PostgreSQL exhaustion increase
- no request timeout
- backend is usable continuously

Do not deliberately launch briefing, Polygon multi-hour ingestion, giant SEC crawls, etc. as
part of this release validation.

That is precisely what we are removing from restart behavior.

==================================================
STEP 6 — KEEP THE DIFF SMALL
==================================================

I do NOT want a 10-service startup framework committed.

After simplification, report the exact production files remaining changed.

If more than roughly 3-4 production files remain changed, STOP before commit and explain why.

Expected shape should be close to:

- backend/main.py
- backend/services/watchlist_rss_sweeper.py

Potentially one existing small service file ONLY if a trivial skip-initial control is necessary.

The previously added broad run_once/control changes should not survive merely because they
already have tests.

Delete/revert tests that exist only for the abandoned coordinator architecture.

Keep focused regression tests for the actual retained fixes.

==================================================
STEP 7 — COMMIT / PUSH
==================================================

If the simplified runtime validation passes:

- git diff --check
- run focused tests
- inspect full diff
- stage exact source/test files only
- exclude caches, snapshots, reports, attachments, memory, runtime files
- create ONE clean commit
- push main
- verify origin/main

Do not republish automatically.

Final answer should say:

READY_TO_REPUBLISH_BACKEND = YES

if the simplified backend is responsive and the clean commit is pushed.

If it fails, report ONLY the request/job that is still running despite the rule that heavy
startup catch-up is disabled.

Do not start another architecture project.