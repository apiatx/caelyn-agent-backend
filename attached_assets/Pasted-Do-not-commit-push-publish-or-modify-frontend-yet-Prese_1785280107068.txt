Do not commit, push, publish, or modify frontend yet.

Preserve the three corrective working-tree reverts already made:

- keep original imported CSV placeholder fields until a safe hydration lifecycle exists
- remove the watchlist.csv_data mutation from upsert_snapshot()
- restore profile and underscore-prefixed fields in bulk rows
- continue stripping only earnings_intelligence from bulk csv_data

There is still one critical backend regression to fix.

Proven symptom:

After get_snapshots_bulk runs for the Watchlist, ticker-detail can read the same shared/warmed snapshot cache and receive a bulk-projected snapshot with earnings_intelligence removed.

This is not harmless or merely pre-existing behavior. It directly explains why the ticker popup Earnings tab disappears after the Watchlist loads.

Required architecture contract:

1. Bulk Watchlist requests may use a lightweight projection that excludes earnings_intelligence.
2. Ticker-detail requests must always receive the complete canonical snapshot, including earnings_intelligence and all About/company fields.
3. A lightweight bulk projection must never overwrite, populate, or masquerade as a complete ticker-detail cache entry.
4. Do not restore earnings_intelligence to the bulk Watchlist payload.
5. Do not add a new endpoint, provider call, table, worker, scheduler, dependency, or duplicate source of truth.
6. Do not solve this in frontend.
7. Do not add continuous request-path FMP calls.
8. Preserve the existing singular get_snapshot detail path.

Audit the exact existing cache behavior:

- get_snapshots_bulk()
- get_snapshot()
- every shared in-memory snapshot cache
- every disk/LKG snapshot cache
- cache population from bulk reads
- cache population from singular reads
- whether bulk rows overwrite full rows
- whether a missing earnings_intelligence key is interpreted as “complete cached snapshot”
- whether profile/About fields can be lost through the same projection path

Prove the exact request sequence:

A. Restart/cold cache.
B. Call the main Watchlist bulk endpoint.
C. Immediately call /api/watchlist/ticker-detail/GLW.
D. Show whether earnings_intelligence is present.
E. Repeat for AMKR and AMD.

Then test the reverse order:

A. Cold cache.
B. Call ticker-detail/GLW first.
C. Call the bulk Watchlist endpoint.
D. Call ticker-detail/GLW again.
E. Confirm the complete detail snapshot was not replaced by a stripped bulk projection.

Make the smallest supported correction.

Preferred behavior, subject to repository evidence:

- get_snapshots_bulk may return stripped projections to the bulk caller
- stripped bulk projections must not be written into the shared complete-detail cache
- get_snapshot must fetch/read the full canonical snapshot through the existing singular path
- if the current shared cache distinguishes complete versus projected rows using an existing mechanism, use it
- do not invent a new cache or status system

Do not simply add earnings_intelligence back to the bulk SQL query, because that would restore the 15+ MB Neon transfer and 19.8 MB Watchlist response.

Validation requirements:

1. Call bulk Watchlist, then ticker-detail GLW:
   - Overview data present
   - About/company data present
   - earnings_intelligence present
   - earnings_history present
   - Q2 2026
   - July 28, 2026
   - EPS actual 0.78
   - revenue actual 4,738,000,000

2. Repeat for AMKR:
   - Q2 2026
   - EPS actual 0.70
   - revenue actual approximately 1,897,965,000

3. Repeat for AMD:
   - About/company data present
   - earnings history present

4. Test both request orders:
   - bulk then detail
   - detail then bulk then detail

5. Confirm Upcoming Earnings and Recent Earnings both return successfully.

6. Confirm bulk csv_data contains zero earnings_intelligence keys.

7. Confirm ticker-detail still contains full earnings_intelligence.

8. Confirm the bulk payload remains approximately 5.4 MB or smaller and does not return to 19.8 MB.

9. Measure five warm bulk requests and five ticker-detail requests.

10. Open the actual preview UI after loading the Watchlist:
    - Watchlist completes loading
    - Upcoming Earnings opens
    - Recent Earnings opens
    - GLW popup shows About
    - GLW popup shows the Earnings tab
    - GLW Earnings Overview shows Q2
    - AMKR and AMD popups remain intact
    - browser console and Network panel contain no failed or timed-out requests

Run all relevant tests and git diff --check.

Then show:

git status -sb
git diff --name-only
git diff --stat
git diff

The final commit should include:

- the three existing corrective reverts
- the narrow cache-projection fix
- focused regression tests for bulk-then-detail cache behavior

Stage only exact source/test files.
Create one local commit.
Do not push.
Do not publish.
Report the commit SHA and exact files changed.