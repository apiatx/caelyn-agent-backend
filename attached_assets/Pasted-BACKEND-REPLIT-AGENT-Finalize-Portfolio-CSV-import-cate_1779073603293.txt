BACKEND REPLIT AGENT — Finalize Portfolio CSV import categorization + persistence

The import diagnostics are now much better. The engine correctly distinguishes:
- csv_lot
- existing_db_holding
- manual_required / needs_basis_review

Good.

But the original user problem is not just diagnostics. The actual Portfolio Dashboard categories must be correct after import:
- Open Positions
- Partially Closed Positions
- Fully Closed Positions
- Monthly closed positions
- Closed trades
- Current active holdings in Neon
- Terminal active holdings

Current known expected examples from the actual CSV:
1. SIVEF
- Open Positions: YES
- Partially Closed Positions: YES
- Fully Closed Positions: NO
- Reason: 2,996 shares remain after 1,000 shares sold.
- Closed trade should show the 1,000-share sell.
- It must not appear as a fully closed May position.

2. OUST
- Open Positions: YES
- Partially Closed Positions: NO
- Fully Closed Positions: NO
- Reason: prior lot was fully closed, then a fresh 302-share lot opened later.
- It should not be partially closed just because the ticker had historical sells.

3. ALMU
- Open Positions: YES
- Partially Closed Positions: NO
- Fully Closed Positions: NO
- Reason: prior lot was fully closed, then a fresh 439-share lot opened later.

Main task:
Verify and fix the actual persisted output that the frontend uses. Do not stop at test-suite diagnostics.

Audit:
1. Find exactly what Portfolio Dashboard reads for:
   - Open Positions
   - Partially Closed Positions
   - Fully Closed Positions
   - Closed Positions by month
2. Confirm whether those sections read from:
   - active holdings Neon table
   - closed_trades Neon table
   - import response only
   - localStorage
   - old stale endpoint
   - cached bad records
3. Confirm the new ledger import endpoint writes the correct derived records into the exact stores those UI sections consume.
4. Confirm stale records from old bad imports are removed when full_replace=true.
5. Confirm no old closed-trade records for OUST/ALMU/SIVEF survive from the old broken importer.

Critical requirement:
full_replace=true must produce a clean rebuild:
- wipe/replace active holdings generated by the import
- wipe/replace closed trades generated by prior CSV imports
- do not leave stale partial/fully closed records from old import runs
- preserve manually entered closed trades only if there is a deliberate source/type field that distinguishes manual vs csv_import; otherwise report the risk clearly

If needed, add a source field:
- source = "csv_import"
- import_batch_id
- source_file
- transaction_fingerprint

So future full_replace can delete only prior csv_import records instead of manual records.

Required persisted output:
After importing the actual CSV with full_replace=true:

A. Active holdings / Open Positions
Return the exact active holdings persisted to Neon:
- count
- symbols
- shares
- avg_cost
- cost_basis
- entry_date
- classification/status

B. Partially Closed Positions
Return exact records/symbols:
- symbol
- shares_remaining
- shares_sold
- realized_pnl
- realized_pnl_pct
- last_sell_date
- status

SIVEF should be here.
OUST should NOT be here.
ALMU should NOT be here.

C. Fully Closed Positions
Return exact records/symbols:
- symbol
- total_shares_bought
- total_shares_sold
- realized_pnl
- realized_pnl_pct
- final_exit_date
- month bucket
- status

SIVEF should NOT be here if shares remain.
OUST should NOT be here if a fresh lot is currently open and the UI’s fully closed section means “currently no open shares.”
ALMU should NOT be here if a fresh lot is currently open.

Important nuance:
If the app wants to show historical fully closed lots separately, label that as “Closed Trades / Historical Lots,” not “Fully Closed Positions” for currently-open tickers. The user-facing “Fully Closed Positions” should mean final current state has no shares remaining.

D. Monthly closed positions
Monthly closed activity can include sell events, but it must distinguish:
- partial sell
- lot close with later reopen
- final full close of ticker

Do not put SIVEF in May fully closed if SIVEF still has remaining open shares.
Do not put OUST/ALMU in partially closed if their current open positions are fresh reopened lots.

E. Closed trades
Closed trades should have one row per sell event or one row per closed lot, depending existing UI design, but each must include:
- symbol
- exit_date
- shares_sold
- exit_price
- avg_cost_at_sale
- realized_pnl
- realized_pnl_pct
- remaining_shares_after
- is_full_close_event
- final_symbol_status_after_import: open / partially_closed_open / fully_closed
- basis_source: csv_lot / existing_db_holding / manual_required

Missing basis behavior:
For needs_basis_review symbols:
- keep them out of accurate realized P&L totals unless the UI clearly labels them as missing basis
- do not silently zero P&L as if it were accurate
- return basis_warning
- return needs_basis_review_symbols

Validation on actual CSV:
Run the actual CSV import into a clean test state or with full_replace=true.

Then call the same endpoints the frontend uses and return:

1. Open Positions endpoint/store:
- includes SIVEF, OUST, ALMU?
- exact shares for each

2. Partially Closed endpoint/store:
- includes SIVEF?
- excludes OUST?
- excludes ALMU?

3. Fully Closed endpoint/store:
- excludes SIVEF?
- excludes OUST?
- excludes ALMU?

4. Monthly closed positions:
- SIVEF May entry is partial sell, not fully closed
- OUST/ALMU historical lot closes are not incorrectly labeling current ticker as partially closed

5. Closed trades:
- SIVEF sell exists with correct P&L
- OUST old lot close exists if expected, but current status is open only
- ALMU old lot close exists if expected, but current status is open only

6. Current active holdings after import:
- count
- symbols
- no stale bad records

7. Terminal after import:
- active holdings match open positions
- closed trades included in trade-history chart where appropriate
- no stale closed/full/partial classifications

Return:
- exact root cause if categories are still wrong
- exact files changed
- import_batch_id/source strategy
- final category table for SIVEF, OUST, ALMU
- full counts for open / partially closed / fully closed / closed trades / needs_basis_review