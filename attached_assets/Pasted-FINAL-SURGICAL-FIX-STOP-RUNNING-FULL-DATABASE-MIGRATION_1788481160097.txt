FINAL SURGICAL FIX — STOP RUNNING FULL DATABASE MIGRATIONS ON EVERY BOOT

We have the actual remaining startup blocker.

Do NOT investigate another background service.
Do NOT touch briefing.
Do NOT reintroduce the coordinator.
Do NOT add another scheduler.
Do NOT expand the production-file set beyond what is explicitly approved here.

CURRENT CLEAN WORKING-TREE TARGET
=================================

Preserve the already-simplified/proven changes currently retained in:

- backend/main.py
- backend/services/watchlist_rss_sweeper.py
- backend/data/caelyn_terminal.py

Authorize ONE final production file:

- backend/data/pg_storage.py

Maximum final production files: FOUR.

==================================================
PROVEN ROOT CAUSE
==================================================

Fresh PID #2 was started with:

- provider-heavy restart catch-up disabled
- no /health requests
- no Watchlist requests
- no validation traffic

Yet required startup hydration still failed to finish after nearly FOUR MINUTES.

The remaining path is:

_deferred_sync_startup
    ->
_init_postgres_chat_storage_on_startup
    ->
PG_STORAGE.init_tables()

Git confirms init_tables() is a giant schema-maintenance routine, not hydration.

It executes a large sequence of:

- CREATE TABLE IF NOT EXISTS
- CREATE INDEX IF NOT EXISTS
- ALTER TABLE ADD COLUMN IF NOT EXISTS
- retention-rule INSERTs
- chat schema
- Watchlist schema
- fundamentals schema
- options schema
- calendar schema
- strategy schema
- theme override schema
- alert schema
- earnings schema
- other application tables

and commits near the END.

Calling this entire DDL suite on EVERY web-process restart is unacceptable.

"Idempotent" does not mean "cheap."

PROCESS RESTART MUST NOT MEAN:
"re-run every schema migration ever created."

==================================================
TASK 1 — ADD A TRUE SCHEMA-CURRENT FAST PATH
==================================================

Modify backend/data/pg_storage.py narrowly.

init_tables() must first determine whether the CURRENT deployed schema already contains the
tables/columns/index prerequisites represented by the existing migration body.

If schema is already current:

- DO NOT execute the giant DDL body
- return True immediately
- log:
  [PG_STORAGE] init_tables schema_current=true action=skip_ddl elapsed_ms=...

This check must be lightweight.

Preferred shape:

ONE bounded information_schema / pg_catalog query (or a very small fixed number of queries),
not one network round-trip per table.

Build the required manifest from the ACTUAL existing init_tables() body.

At minimum verify:

- required tables exist
- columns added via ALTER TABLE exist

Do not require index-by-index verification if that would turn the fast path back into a giant
catalog crawl unless an index is correctness-critical.

Do not invent schema version infrastructure in this incident.

==================================================
TASK 2 — PRESERVE THE EXISTING MIGRATION BODY
==================================================

If the lightweight schema check detects genuine missing schema:

run the EXISTING init_tables DDL body.

Do NOT rewrite the migrations.

Do NOT change table definitions.

Do NOT remove CREATE/ALTER statements.

Do NOT change persistence contracts.

This gives us:

normal restart:
    cheap schema check -> skip

actual schema drift:
    existing migration logic -> repair

==================================================
TASK 3 — SINGLE-FLIGHT WITHIN THE PROCESS
==================================================

Prevent two callers in the same process from running init_tables() simultaneously.

Use a simple existing-language process lock/state.

Requirements:

- first caller owns initialization
- concurrent caller waits for or observes the same result
- after successful completion, later calls return immediately
- failed initialization may be retried intentionally

Do not create a distributed lock system.
Do not create a migration service.

==================================================
TASK 4 — HEALTH MUST NEVER RUN MIGRATIONS
==================================================

Audit backend/main.py specifically for /health and initialization behavior.

The previous contaminated run proved a /health request could cause a SECOND PostgreSQL
initialization attempt.

That must never happen.

GET /health:

MUST NOT:
- call init_tables()
- call _init_postgres_chat_storage_on_startup()
- create tables
- alter tables
- seed schema
- wait minutes for schema migration

Health is observational.

It may perform a tightly bounded lightweight DB connectivity check if that is already part of
the contract.

Preserve existing response fields as much as possible.

If initialization is still in progress, expose that as state.

Do not make an HTTP health probe mutate the database.

GET /ping must remain completely trivial.

==================================================
TASK 5 — TIME THE STARTUP STEPS
==================================================

Add concise timing around:

- PostgreSQL connectivity probe
- schema-current check
- init_tables total
- entire _deferred_sync_startup

No stack dumps.
No noisy per-DDL logging.

We need proof that the normal current-schema path is fast.

==================================================
TASK 6 — VERIFY THE LIVE DATABASE IS ALREADY CURRENT
==================================================

Before relying on the fast path, inspect the actual Neon schema read-only.

Confirm whether the tables/columns required by the CURRENT init_tables() body already exist.

Report:

schema_current = true/false

If false:

- name the missing table/column(s)
- run the EXISTING migration routine ONCE in the dev environment
- verify the schema afterward
- restart again

Do not weaken the schema check merely to force a pass.

==================================================
TASK 7 — TESTS
==================================================

Focused tests only.

Prove:

1. CURRENT SCHEMA
   schema current -> init_tables skips DDL

2. SCHEMA DRIFT
   missing required table/column -> existing DDL path runs

3. REPEATED CALL
   second successful call does not rerun DDL

4. CONCURRENT CALL
   two callers cannot execute migration body concurrently

5. FAILED MIGRATION
   failure does not permanently mark initialization successful

6. HEALTH
   /health cannot invoke init_tables or startup schema mutation

7. PING
   /ping has no DB dependency

Preserve the existing 46 focused tests from the simplified incident fix.

==================================================
TASK 8 — FINAL RUNTIME VALIDATION
==================================================

Fresh PID.

THIS TIME start probes immediately once the HTTP server accepts connections.

That is intentional.

We need to prove health probes themselves are safe.

Run:

20 consecutive /ping
20 consecutive /health

Requirements:

- 20/20 ping
- 20/20 health
- no timeout
- health causes ZERO schema-init calls
- only one startup schema initialization attempt exists
- current-schema fast path skips DDL

Required hydration:

- completes quickly
- report exact elapsed time

Then:

PRIMARY WATCHLIST
- discover canonical count/hash at runtime
- 10 consecutive requests
- 10/10 HTTP 200
- correct membership
- no timeout
- warm path fast

HOME MATRIX
- Home dashboard
- Daily Alpha
- Risk Intelligence
- Trading Dashboard
- Theme RS
- Prediction Markets
- News

All HTTP 200.

Keep process running for 10 minutes.

Confirm forbidden provider-heavy restart catch-up remains absent.

Record:

- CPU
- threads
- PG pool held
- exhaustion delta
- Tradier usage

==================================================
ACCEPTANCE
==================================================

Normal current-schema boot target:

- schema check should be seconds at worst, preferably milliseconds
- NOT minutes

No PostgreSQL exhaustion increase caused by startup.

No request timeout.

No heavy provider restart catch-up.

==================================================
GIT
==================================================

Final production-file maximum:

1. backend/main.py
2. backend/services/watchlist_rss_sweeper.py
3. backend/data/caelyn_terminal.py
4. backend/data/pg_storage.py

No fifth production file.

Delete abandoned coordinator-only tests.

Keep only tests supporting retained fixes.

Then:

- git diff --check
- focused tests
- inspect complete staged diff
- stage exact source/test files only
- no caches/snapshots/reports/memory/runtime files
- one clean performance closure commit
- push main
- verify origin/main

Do not republish automatically.

If runtime passes, end:

READY_TO_REPUBLISH_BACKEND = YES

If it fails, report ONLY:

- the exact startup function still running
- its elapsed time
- why it remains required

Do NOT launch another architecture investigation.